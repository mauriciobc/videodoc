# Voiceover (Google Cloud Text-to-Speech)
# Copy to .env and fill in one of the options below.
# Used by: npm run docs:voiceover, npm run docs:render, npm run docs:generate

# Option A — Service account JSON (recommended for CI/CD)
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/your-service-account.json

# Option B — API key (simpler for local use)
# GOOGLE_TTS_API_KEY=your-api-key
